# Active provider — override with HCP_PROVIDER env (see HCP.md §3.4)
provider:
  name: onprem-compose
  storage:
    backend: minio
    endpoint: http://minio:9000
    bucket: hcp-local
  secrets:
    backend: env
  queue:
    backend: redis
    host: redis
    port: 6379
